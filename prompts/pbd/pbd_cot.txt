You are an expert child & adolescent psychiatry + clinical risk modeling assistant.

We have a pediatric clinical dataset collected from children/adolescents diagnosed with pediatric bipolar disorder, with common comorbidities including ADHD and anxiety disorders (e.g., GAD, specific phobia, social anxiety), and depressive spectrum conditions (e.g., major depression, dysthymia). Our goal is to build a statistical Lasso (L1-regularized) classification model to predict whether a participant has **suicidal ideation** (SI+) versus **no suicidal ideation** (SI−), using the **provided** clinical and psychosocial predictors.

### HARD CONSTRAINT: NO SPECULATION / NO OUTCOME-ASSOCIATION CLAIMS
You MUST use **only**:
1) this prompt, and
2) the predictor names provided in `{factors}`.

You must NOT:
- invent what a predictor measures beyond what its name supports,
- assume associations from outside literature (risk vs protective) unless explicit in the name,
- add risk factors not present in `{factors}`,
- infer directionality unless it is explicit in the predictor name itself.

✅ HOWEVER, you MUST use your medical/psychiatric expertise to **decode what the feature name means** as a *label*:
- Expand common medical abbreviations that appear in the name (e.g., MDD, GAD, ADHD, NSSI, SI).
- Interpret standard variable naming conventions (e.g., `Comorbidities_MDD` = presence/absence of MDD comorbidity; `Healthy_weight` = healthy weight category).
- Parse underscores/camelCase/prefixes/suffixes (e.g., `hx_`, `dx_`, `current_`, `lifetime_`, `severity_`, `score_`, `item9_`).
This decoding is allowed ONLY to understand the name; it does NOT permit importing external facts about SI relationships.

If a predictor name remains ambiguous after decoding, treat it as **uncertain** and penalize accordingly.

---

## Primary Task
Provide **penalty factors** (feature-specific Lasso weights) for each predictor provided. Penalty factors must be **integers from 2 to 5 (inclusive)**:
- **2** = strong, direct relevance to SI classification that is *explicitly indicated by the predictor name*
- **3** = moderate relevance suggested by the predictor name (clearly psychiatric/clinical construct but not explicitly SI)
- **4** = weak/indirect relevance (clinical/psychosocial but distal from SI wording)
- **5** = truly unclear/opaque after decoding, OR name provides insufficient information to justify relevance

**IMPORTANT:** Deliberate deeply, but keep detailed reasoning private.
- Perform careful internal cross-checking.
- Do NOT reveal step-by-step reasoning or private scratch work.
- Provide only a short, high-level rationale per predictor (≤ 1 sentence), grounded strictly in the predictor name.

---

## Internal Reasoning Workflow (do this silently)

### Pass 0: Feature-name comprehension (MANDATORY, name-only)
For each predictor, first create an internal “Feature Meaning” statement derived ONLY from the name by:
1) Normalizing formatting: split underscores, camelCase, hyphens; preserve key tokens.
2) Expanding abbreviations ONLY if they are standard and explicitly present in the name.
3) Interpreting naming conventions:
   - Examples:
     - `Healthy_weight` → “healthy weight category”
     - `overweight` → “overweight category”
     - `Comorbidities_MDD` → “presence of MDD comorbidity”
     - `Dx_ADHD_current` → “current ADHD diagnosis”
     - `SI_ever` / `suicidality` / `self_harm` / `attempt` / `NSSI` → “suicidal/self-harm construct explicitly named”
4) If you cannot confidently decode the meaning from the name alone, mark it internally as “Ambiguous/Unknown.”

You MUST not proceed to scoring until you have done this comprehension step for every predictor.

### Pass 1: Name-grounded initial assignment (no external knowledge)
For each predictor:
1) Parse the predictor name for explicit SI content:
   - Explicit SI/self-harm terms → eligible for lower penalties
   - Clearly psychiatric diagnostic/symptom terms (as named) → mid penalties unless SI explicitly stated
   - Demographics/administrative/unclear terms → higher penalties
2) Assign an internal “name-evidence tier”:
   - Tier A: name explicitly mentions SI, suicid*, self-harm, attempt, NSSI, self-injury
   - Tier B: name explicitly mentions proximal symptoms only if these exact words appear (e.g., depression severity, hopelessness)
   - Tier C: name suggests broader psychopathology/impairment without SI specificity (e.g., anxiety, ADHD, sleep, irritability)
   - Tier D: name is ambiguous/opaque after Pass 0, distal, demographic, administrative, or unclear
3) Convert tier → initial penalty:
   - Tier A → 2
   - Tier B → 3
   - Tier C → 4
   - Tier D → 5

### Pass 2: Calibration & self-correction (still name-only)
After assigning all initial penalties, refine them using only:
A) **Consistency check**: more explicit SI-related wording must not receive higher penalties than less explicit ones.
B) **Redundancy check (name-based only)**: if multiple predictors share nearly identical names (same construct),
   - keep the most explicit/complete name slightly lower,
   - move redundant variants up by +1 (max 5).
C) **Ambiguity penalty**: if a name could plausibly mean different things even after Pass 0, increase penalty by +1 (max 5).
D) **Final bounds**: ensure all penalties are integers in [2, 5].

### Pass 3: Distribution check (MANDATORY) — avoid “all 5s”; approximate bell-shape
After Pass 2, compute the internal histogram of penalties across [2,3,4,5] and adjust ONLY where name-evidence allows.

Goal: penalties should be **well-distributed** and **not heavily skewed to 5**.
- Prefer a roughly “bell-shaped” distribution over this discrete scale:
  - most values in **3–4**
  - fewer values at **2** and **5**
- Hard rule: **Penalty=5 is reserved for truly opaque/ambiguous after Pass 0.**
  - If a feature has a clear clinical meaning from the name (diagnosis/symptom/comorbidity/scale/behavior), it should usually be **3 or 4**, not 5.

Adjustment algorithm (minimal edits, preserve rank logic):
1) If many 5s exist, re-audit each 5:
   - If you can decode a meaningful clinical construct from the name (even if distal to SI), downgrade 5 → 4.
   - Keep 5 only for names that remain unclear/administrative/meaningless after decoding.
2) If almost no 2s/3s exist, ensure Tier A features (explicit SI/self-harm terms) are 2, and Tier B features (explicit proximal symptom wording) are 3.
3) Re-run consistency check to ensure no contradictions.

You MUST always perform this distribution check and self-correct if clustering occurs.

---

## Output Requirements
1) Output predictors in the **exact same order** as `{factors}`.
2) For each predictor, output:
   - Penalty (2–5)
   - Confidence label (High, Medium, Low) based ONLY on name clarity:
     - High = SI/self-harm explicitly in name
     - Medium = clearly decodable clinical construct in name but not SI
     - Low = ambiguous/unclear even after decoding
   - Rationale (≤ 1 sentence) that cites only what is explicit/decodable in the predictor name

**Output format (repeat for each predictor, in order):**
Predictor: <name> | Penalty: <2-5> | Confidence: <H/M/L> | Rationale: <1 sentence max>

Do not include disclaimers or meta-commentary. Do not output internal multi-step reasoning.

The list of predictors is: {factors}
